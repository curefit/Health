name: screen_state_example
description: Demonstrates how to use the screen_state plugin.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 5.0.0+1

environment:
  sdk: ^3.10.0
  flutter: ">=3.6.0"

dependencies:
  flutter:
    sdk: flutter
  screen_state:
    path: ../
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
